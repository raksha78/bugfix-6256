pipelines:
 - name: pipeline_2
   steps:
     - name: step_2
       type: Bash
       configuration:
         inputResources:
           - name: "myAppRepoall"
       execution:
         onExecute:
           - echo $MY_MESSAGE
