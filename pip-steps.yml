pipelines:
 - name: pipeline_1
   steps:
     - name: step_1
       type: Bash
       configuration:
         inputResources:
           - name: myAppRepo
       execution:
         onExecute:
           - echo $MY_MESSAGE
